{
  "scripts": {
    "test": "vitest"
  }
}

import { describe, it, expect } from 'vitest';

describe('App', () => {
  it('should work', () => {
    expect(1 + 1).toBe(2);
  });
});

import { beforeEach } from 'vitest';

beforeEach(() => {
  // Setup code can go here
});